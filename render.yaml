services:
  - type: web
    name: proyecciones-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api_render:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: API_KEY
        sync: false
      - key: FRONTEND_ORIGINS
        sync: false
  - type: web
    name: proyecciones-streamlit
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run ProyAst.py --server.address 0.0.0.0 --server.port $PORT --server.headless true
    envVars:
      - key: LLM_PROVIDER
        value: anthropic
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: ANTHROPIC_MODEL
        value: claude-3-5-sonnet-latest
      - key: ACCESS_INITIAL_USER
        value: Admin
      - key: ACCESS_INITIAL_PASSWORD
        sync: false
      - key: API_URL
        sync: false
      - key: API_KEY
        sync: false
